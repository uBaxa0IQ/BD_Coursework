# План исправления замечаний — по уровням сложности

Обозначения:
- **Критичность** — насколько вероятно, что замечание «всплывёт» на защите / в нормоконтроле, если не поправить.
- **Сложность** — сколько головы и времени реально нужно.

Общая оценка: текстовых замечаний 65 (многие — повторы одной правки), плюс ~30 ручных пометок на схемах. По сути уникальных задач **~20**. Большинство — механика и косметика, тяжёлого думанья требуют 5–6 пунктов. Грубо: **12–16 часов** работы, из них «думать» — часа 4–5, остальное руки.

---

## Уровень 1. Содержательное — переписывать с головой

### 1.1. Доказательство нормальных форм (стр. 27, замечания №19–22)
**Критичность: ВЫСОКАЯ. Сложность: ВЫСОКАЯ. ~2–3 ч.**

Самое серьёзное место — руководитель забраковал почти весь подраздел 2.1.1. Корень ошибки: ты рассуждаешь про «первичный ключ», а нормальные формы проверяются относительно **потенциальных** (candidate) ключей.

Как фиксить — переписать по жёсткой схеме, отдельно по каждой таблице:
1. Перечислить **все потенциальные ключи**. У тебя их почти везде больше одного:
   - `seasons`: `season_id`, `label`
   - `teams`: `team_id`, `nba_team_id`, `name`, `abbreviation`
   - `players`: `player_id`, `nba_id`
   - `positions`: `position_id`, `code`
   - `game_player_stats`: `stat_id` и `(game_id, player_id)`
   - `player_season_stats`: `pss_id` и `(player_id, season_id, team_id)`
2. Выписать **неключевые атрибуты** (не входящие ни в один потенциальный ключ) — именно этого просит замечание №21.
3. **2НФ**: показать, что неключевые зависят от *всего* составного ключа, а не от части. Реально проверять надо только таблицы с составным потенциальным ключом (`game_player_stats`, `player_season_stats`).
4. **3НФ**: показать отсутствие транзитивных зависимостей «неключевой → неключевой» по каждой таблице.
5. Абзац про витрину (№22) — переформулировать. Сейчас написано «нарушает 3НФ, т.к. зависит от множества строк таблицы фактов» — это **методологически неверно**: 3НФ описывает функциональные зависимости между атрибутами *внутри одной* таблицы, а не зависимость от строк другой таблицы. Правильная формулировка: витрина — это сознательная денормализация, хранящая *вычисляемые/производные* показатели, что вводит избыточность относительно таблицы фактов; это осознанный отказ от принципа «не хранить вычисляемое ради скорости чтения», а не формальное нарушение 3НФ.

Удобно оформить компактной таблицей: «таблица → потенциальные ключи → неключевые атрибуты → вывод по 2НФ/3НФ».

### 1.2. Анализ СУБД вместо сравнения (стр. 36, замечание №40)
**Критичность: ВЫСОКАЯ. Сложность: СРЕДНЯЯ. ~1–1.5 ч.**

Руководитель прямо ссылается на методичку: «ошибка — когда вместо *анализа* СУБД только их *сравнение*», плюс «непонятно, почему именно эти критерии».

Как фиксить:
- Перед таблицей 3.1 добавить абзац, **откуда взялись критерии** — они вытекают из главы 2:
  - PL/pgSQL ⟵ вся логика метрик вынесена в хранимые функции (раздел 2.3);
  - DEFERRABLE-триггеры ⟵ проверка суммы очков возможна только на COMMIT (раздел 2.2);
  - asyncpg ⟵ асинхронный сервер с конкурентной нагрузкой (раздел 2.5).
- По каждой из трёх СУБД дать абзац-анализ (что умеет/не умеет относительно этих требований), а таблицу оставить как сводку.
- Аналогично желательно усилить обоснование Redis / FastAPI / React — «почему», а не только «что».

### 1.3. Методика замеров (стр. 47, замечание №48)
**Критичность: СРЕДНЯЯ. Сложность: НИЗКАЯ (если есть числа) / ВЫСОКАЯ (если переделывать замеры).**

Нужно в 4.1 описать процедуру: сколько прогревочных итераций, сколько рабочих прогонов, отбрасывались ли крайние значения и сколько — *прежде* чем считались медиана/среднее/P95.

> **Нужно от тебя:** реальные параметры из скрипта замеров. Без них честно не напишу.

### 1.4. Вывод про O(log N) по трём точкам (стр. 47, замечание №50)
**Критичность: СРЕДНЯЯ. Сложность: НИЗКАЯ. ~15 мин.**

Руководитель прав: по трём точкам нельзя заявлять класс сложности. Убрать «подтверждает логарифмическую сложность O(log N)». Замена:
> «Медиана практически не растёт (0,20–0,23 мс) при увеличении объёма витрины в 5,4 раза, что согласуется с применением B-tree индекса — поиск нужных строк идёт без полного сканирования таблицы.»

Без претензии на конкретный класс сложности.

### 1.5. «500 строк маловато для оценки индекса» (стр. 47, замечание №51)
**Критичность: СРЕДНЯЯ. Сложность: СРЕДНЯЯ–ВЫСОКАЯ.**

Витрина мала по природе (агрегат), поэтому эксперименты 4.2 на ней нерепрезентативны. Два пути:
- **(а, сильнее)** прогнать сравнение стратегий индексации на *таблице фактов* (150 629 строк), а не на витрине — там разница будет наглядной;
- **(б, дешевле)** явно оговорить ограничение и сослаться на 4.3 (где агрегация уже идёт по 32 382 фактам) как на «настоящий» индексный эксперимент.

> **Нужно от тебя:** готов ли догнать эксперимент на фактах, или фиксим текстом.

### 1.6. Схемы алгоритмов — формальная корректность (Приложение А, замечания №57–62, 64–65)
**Критичность: СРЕДНЯЯ. Сложность: СРЕДНЯЯ (перерисовка 8 схем).**

Несколько правок сразу:
- Блоки «Суммирование PTS, FGA, FTA…» → показать **присваивание в переменные**: `v_pts ← Σ PTS`, `v_fga ← Σ FGA` и т.д. (замечание «куда идёт сумма» повторено 6 раз — №57–60, 62, 64).
- `denom` (№61) — подписать «знаменатель» или ввести явной формулой.
- В схеме триггера добавить блок «получить home_score / away_score из games» — тогда «ожидаемый счёт» (№65) не возникает из ниоткуда.
- Рис. А.8 (№66): «начало транзакции / конец транзакции» → просто «начало» / «конец»; **блоки одного размера**; вызовы `calculate_*` оформить как «предопределённый процесс» (блок с двойными боковыми линиями по ГОСТ 19.701).
- 30 ручных пометок на рис. 1.1, 2.3, А.4–А.6 — это, скорее всего, те же замечания, нарисованные от руки. Смотреть по PDF, править заодно.

> **Нужно от тебя:** в чём нарисованы схемы (TikZ прямо в LaTeX или внешний редактор) — от этого зависит способ правки.

---

## Уровень 2. Нудно, но без раздумий — проход по всему документу

### 2.1. Убрать SQL/PostgreSQL-термины до выбора СУБД
**Критичность: СРЕДНЯЯ (повторено ~14 раз — придерётся). Сложность: НИЗКАЯ–СРЕДНЯЯ. ~1 ч.**

Замечания №12–17, 23–35. Сквозная замена в Аналитическом и Конструкторском разделах:

| Было | Стало |
|------|-------|
| `UNIQUE` | уникальный / уникальность |
| `JOIN` | объединение |
| `CHECK` | ограничение проверки (или убрать) |
| `(LIMIT)`, `(ORDER BY)` | убрать скобки |
| `UPSERT`, `ON CONFLICT DO UPDATE` | вставка с обновлением |
| `CONSTRAINT TRIGGER … DEFERRABLE` | отложенная проверка на завершении транзакции |
| `REFERENCING NEW TABLE` | переходные таблицы |
| «B-tree — стандартная структура PostgreSQL» | сбалансированное дерево |

Найти-заменить + лёгкая переформулировка. **Важно:** руководитель отдельно написал «и на схеме тоже» — значит реляционную схему (рис. 2.1) тоже почистить от `UNIQUE`/`PK`/`FK`-нотации либо вынести её в Конструкторский после оговорки.

### 2.2. Ссылка на каждую таблицу / рисунок / листинг в тексте *выше* объекта
**Критичность: СРЕДНЯЯ (формальное требование, лёгкая придирка). Сложность: НИЗКАЯ, но объёмно. ~1 ч.**

Замечания №6, 37, 46, 49, 52, 54 + явное «дальше не пишу, проверьте все». Пройти весь документ сплошняком, перед каждым объектом без вводки добавить «В таблице X.Y приведено…», «На рисунке X.Y показано…», «В листинге X.Y…».

### 2.3. Перенести схемы алгоритмов из Приложения А в основной текст
**Критичность: СРЕДНЯЯ (структурное требование). Сложность: СРЕДНЯЯ.**

Замечание №30: «схемы алгоритмов не выносят в приложение, в приложение идут листинги». Переместить А.1–А.8 в раздел 2.3, перенумеровать в рис. 2.X, поправить все ссылки «см. Приложение А (рис. А.1)» → «рис. 2.X». Делать заодно с п. 1.6 (всё равно их трогаешь).

### 2.4. Столбец «формат ответа» в таблицах REST API
**Критичность: НИЗКАЯ–СРЕДНЯЯ. Сложность: СРЕДНЯЯ.**

Замечание №36 (стр. 32). Добавить во все 5 таблиц маршрутов (2.10–2.14) столбец с описанием структуры ответа.

---

## Уровень 3. Локальные содержательные правки

| № в файле | Стр. | Что сделать | Критичность | Сложность |
|-----------|------|-------------|-------------|-----------|
| 3 | 17 | Табл. 1.2: оставить только атомарные показатели из формул (и вычисляемые метрики) с типами; убрать остальное (полные типы данных и так в Конструкторском). Убрать слово «Логические» из шапки (№4) | Средняя | Низкая |
| 5 | 19 | Раскрыть «управление пользователями» — что включает (добавление/удаление/редактирование учёток, изменение схемы), а не пересказ другими словами | Средняя | Низкая |
| 32 | 30 | Описание триггера `trg_validate_team_score_row` вынести из «Алгоритмов расчёта метрик игроков» в подраздел про ограничения целостности | Средняя | Низкая |
| 33 | 30 | К «2.3.2 Агрегирование командной статистики» добавить схему алгоритма (её нет) | Средняя | Низкая |
| 8 | 21 | «Вывод»: переставить абзацы в порядке следования тем по разделу | Низкая | Низкая |
| 44 | 47 | «нужен код запроса» — добавить листинг запроса к эксперименту | Низкая | Низкая |
| 45 | 47 | Написать, что именно индексировалось в «составном индексе» | Низкая | Низкая |

---

## Уровень 4. Косметика — всё вместе минут 30–40

| № в файле | Стр. | Правка |
|-----------|------|--------|
| 2 | 15 | «Цели» → «Задачи» |
| 10 | 22 | «Реляционная схема» → «База данных» |
| 9 | 22 | Подпись рис. 2.1 → «Схема реляционной базы данных»; шрифт на рисунке крупнее |
| 11 | 23 | «Логический тип» → «тип» во всех таблицах |
| 7 | 20 | «По четырём из пяти» → «по всем пяти» |
| 18 | 26 | «UI» — расшифровать или заменить |
| 1 | 11 | Проставить ссылку [n] к абзацу про структуру NBA |
| 38 | 36 | Ссылка к «HTAP» |
| 41 | 37 | Подписи всех листингов выровнять по левому краю (и в приложениях) |
| 42, 43 | 38 | Два пропущенных пробела («в Приложении В», «В, листинг») |
| 55 | 48 | Рис. 4.1: легенда перекрывает диаграмму — сдвинуть |
| 57 | 50 | Пояснить столбец «Ошибки (%)» (что считается ошибкой) |
| 58 | 50 | Добавить абзацные отступы («красные строки») |
| 47 | 50 | Описать, какие запросы и условия (в т.ч. по кэшу) в эксперименте 4.5 |
| 60 | 45 | «физическая [реализация]» — пояснить формулировку |
| 56 | 48 | «Напомните, где приведены эти индексы» — добавить перекрёстную ссылку |
| 53 | 47 | «А тестирование по классам эквивалентности проведено?» — явно написать результат (можно столбец «Результат» в табл. 3.9) |

---

## Что нужно от тебя, чтобы двигаться

1. **Параметры замеров** (прогрев / число прогонов / отбрасывание выбросов) — для п. 1.3.
2. **В чём нарисованы блок-схемы** (TikZ или внешний редактор) — для п. 1.6.
3. **Готов ли догнать индексный эксперимент на 150k фактов** или фиксим текстом — для п. 1.5.

## Рекомендуемый порядок захода

Сначала закрыть Уровни 3–4 (быстро тает список и поднимает мораль), параллельно — Уровень 2.1 (SQL-термины) как самый «придирчивый». Затем Уровень 2.2–2.4. И в конце, на свежую голову — Уровень 1, по одному пункту, начиная с НФ (1.1) как самого важного.