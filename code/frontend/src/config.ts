// Глобальные флаги отображения фронтенда.

/**
 * Показывать командные рейтинги на 100 владений (ORtg / DRtg / NET).
 * false — полностью убирает все три рейтинга со всех экранов команд
 * (плитки сводки и таблица сравнения на странице команды, hero-плитка
 *  лидера по NET, колонки и CSV-экспорт в таблице лидеров команд).
 */
export const SHOW_TEAM_NET_RATINGS = false

/** Ключи метрик, относящихся к командным рейтингам. */
export const TEAM_RATING_KEYS = ['off_rtg', 'def_rtg', 'net_rtg'] as const
